# Updater

A small go project to auto-update MiddleWar


## Build

```
#!/usr/bin/env bash
GOARCH=amd64
URL=http://url/of/archives/

GOOS=linux
go build -o mw-launcher -ldflags "-X main.ROOT_URL=$URL" ui.go

GOOS=windows
go build -o mw-launcher.exe -ldflags "-X main.ROOT_URL=$URL" ui.go

echo "Done"
```